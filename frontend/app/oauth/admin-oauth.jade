sub-header
  admin-oauth-subheader(on-form-submit="$ctrl.save()", form="form")

form(name="form")  
  admin-oauth-twitter(config="$ctrl.config.twitter")
  admin-oauth-facebook(config="$ctrl.config.facebook")
  admin-oauth-github(config="$ctrl.config.github")
