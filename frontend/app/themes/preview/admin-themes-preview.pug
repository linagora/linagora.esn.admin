.card
  .row.nomargin
    .col-md-8.col-xs-12
      .card-header
        h2 #{__('Desktop Preview')}
      .card-body
        admin-themes-preview-desktop(colors="$ctrl.colors", logos="$ctrl.logos")
    .col-md-4.col-xs-12
      .card-header
        h2 #{__('Mobile Preview')}
      .card-body
        admin-themes-preview-mobile(colors="$ctrl.colors", logos="$ctrl.logos")